**IMPORTANT: PHASE 1 - GRID ONLY**

We have a severe rendering issue. The 3D scene shows COMPLETELY BLACK with no visible grid tiles.

**Current state:**
- Grid: 150x150 tiles (22,500 tiles total)
- Tile size: 1x0.08x1 units
- Grid covers 150x150 units area
- Camera: Position [150, 100, 150], zoom 20
- Colors: #555555 (sides), #666666 (tops), #444444 (bottoms)

**Problem:**
The entire viewport is completely BLACK. No grid is visible.

**Suspected issues to fix:**

1. **Camera too far/close:** The orthographic camera might not be capturing the grid. Try:
   - Position: [100, 80, 100] or [80, 60, 80]
   - Zoom: 15-25 range
   - Ensure camera looks at [0, 0, 0]

2. **Materials not rendering:** BoxGeometry materials might be wrong:
   - Try MeshBasicMaterial instead of MeshStandardMaterial (no lighting dependency)
   - Colors should be hex strings like '#666666'
   - Try brighter colors first: #888888, #999999, #AAAAAA

3. **Lighting too dark:** Ambient light intensity 0.5 + directional 1.5 might be insufficient:
   - Try ambientLight intensity: 1.0 or 2.0
   - Try directionalLight intensity: 3.0 or 5.0

4. **Grid not centered:** Ensure grid is centered at origin [0, 0, 0]

**Requirements for Phase 1 (SOLO):**
- Isometric orthographic view of grid ONLY
- NO UI, NO HUD, NO TEXT, NO BUTTONS
- Grid must cover entire viewport without gaps
- Simple wireframe or flat colors for visibility
- Colors: top=#0a0a0a, sides=#050505, bottom=#000000 (final, but use bright colors for debugging first)

**Quick fix for debugging:**
1. Use MeshBasicMaterial with bright colors (white or light gray)
2. Position camera at [50, 40, 50] with zoom 10
3. Set ambientLight intensity to 1.0
4. Remove shadows temporarily (receiveShadow, castShadow)

Get the grid visible first, then we'll adjust colors to the Nothing palette.